# Status — 2026-08-21

Two things shipped: a production LLM client, and a repository that finally describes itself.

**Where it stands:** 929 tests, five checks green, deployed demo live. Transaction persistence is
implemented in the working tree and its migration is applied to the configured PostgreSQL database;
the application changes have not been committed or deployed. No evaluation scores exist yet, and
nothing in the repository claims any.

---

## Shipped today

### `41594cc` — production LLM client

14 files, ~2,600 lines. Sits behind the `LLMProvider` port that already existed, so the categoriser,
hybrid, metrics, and report are unchanged. That was the point of building the port first.

| Module | What it does |
|---|---|
| `errors.py` | Error taxonomy split by whether retrying can help. Includes `529` (Anthropic overload) — not a standard status, and a client that only knows 5xx calls it fatal and fails the batch. |
| `transport.py` | `Transport` port + stdlib `urllib` implementation + `FakeTransport`. The seam that makes everything testable offline. |
| `retry.py` | Full jitter, `Retry-After` honoured over our arithmetic, wall-clock deadline. Injected `Clock` so tests don't sleep. |
| `prompts.py` | Prompt as a versioned artifact (`categorise/v1`), tool schema with the label space closed by an enum. |
| `anthropic.py` | The adapter. Structured output via forced tool use, temperature 0, key redacted from `__repr__`. |
| `factory.py` | Builds from settings; returns `None` rather than raising when no key exists. |
| `llm_smoke.py` | Offline mode prints the exact bytes that would be sent; `--live` makes one real call. |

**Found by running it rather than trusting the tests:** every fixture used the label `DINING`, which
is not in the taxonomy — the real one is `LIVING_DINING`. The tests passed anyway, because the
provider deliberately leaves label validation to `LLMCategoriser`. The fixture error was the
symptom; the actual gap was a missing test at the seam between the client and the harness. That test
now exists, and it documented something worth keeping: `UNKNOWN` is both a valid label and the
abstention sentinel, which lets a model decline *inside* the schema instead of guessing.

### `966b115` — one identity

The README said "OfferDelta — calculates the difference between a current job and a new offer". The
last several commits were transaction ingest, a taxonomy, and an evaluation harness. A reviewer's
first sixty seconds went on working out what this was.

Rewritten around: product problem, architecture and its one hard boundary, the 30-label taxonomy,
rule/LLM/hybrid evaluation, privacy, setup, limitations. It states plainly that no scores exist.

Also wired the live evaluation path, **which turned out not to exist** — `run_evaluation.py` always
used the stand-in and never called `build_provider`, so a checklist telling someone to run a live
evaluation would have been fiction. It now takes `--live`, `--limit N`, and prints a cost estimate
before sending anything.

New: [`docs/LIVE-VALIDATION.md`](../LIVE-VALIDATION.md). Blueprint moved to
[`docs/BLUEPRINT.md`](../BLUEPRINT.md) with a scope header.

---

## Phase 1 gate — verified today

The [scope contract](../planning/PHASE-1-SCOPE.md) listed nine gate items, all unchecked. Checked
against the code today:

| Item | State |
|---|---|
| Live URL reachable by someone who is not you | **met** — `offerdelta.onrender.com` |
| Every result number expands to a derivation | **met** |
| Reconciliation invariant passes every month and band | **met** |
| Property tests green, allocation + break-even | **met** |
| 401(k) change marks the override `STALE` | **met** |
| No monetary value crosses the API as a JSON number | **met** — contract test walks the raw body |
| `pyproject.toml` has no AWS, LLM, or Spark dependency | **met** — the LLM client uses stdlib `urllib` |
| README shows the live URL and what the project does first | **met** as of today |
| **A non-developer completes the comparison without help** | **not met** — there are no input forms |

Eight of nine. The ninth is the honest one: the demo *shows* a comparison, but nobody can put their
own numbers in.

---

## Next move — implemented, not deployed

Imported transactions now have a commit path after preview:

- `transactions` stores exact `NUMERIC` amounts, the original cells, source line, account, and the
  existing fingerprint. The migration is `a6128d6e4f20`.
- Dedupe is multiplicity-aware. `(account, fingerprint, occurrence)` makes a complete re-import a
  no-op without collapsing two real identical charges from the same day.
- `preview_import.py --commit --account=checking` always renders the preview first, refuses partial
  imports when any source row failed, and reports every already-stored source line.
- The migration first compiled to PostgreSQL SQL offline, then advanced the configured database from
  `e438a28dd961` to `a6128d6e4f20`. PostgreSQL integration tests run inside rollback-only transactions
  and leave no imported rows behind.

Verification: 929 tests plus format, lint, strict types, and both architecture contracts — all green.

---

## TODO

### Blocked on you — these unblock everything else

- [ ] **`ANTHROPIC_API_KEY`.** Gates live validation and every evaluation score. Follow
      [LIVE-VALIDATION.md](../LIVE-VALIDATION.md); the whole sequence costs under $2. Nothing else in
      the AI half can progress without it.
- [ ] **The annotation dataset.** 400 rows, ~100 double-annotated, at
      `backend/data/eval/transactions.csv` (gitignored; not yet created). Run
      `uv run python validate_dataset.py` as you go — a misspelled label caught at row 40 costs a
      moment, the same one caught at row 400 costs a re-read of all of them.
- [ ] **A real redacted bank export.** Ingest detection has only ever been tested against layouts I
      invented. One real file is worth more than ten synthetic ones.
- [ ] **Real personal numbers** — salary, paystub net pay, rent, recurring costs. Three `ASSUMED`
      placeholders remain in `demo/profiles.py`.

### Next engineering — unblocked, ready to start

- [x] **Persist imported transactions.** Implemented locally with a schema, occurrence-aware dedupe,
      source lineage, and an explicit commit-after-preview path. Migration applied; code awaits
      commit and deployment.
- [ ] **Input forms.** The last unmet phase-1 gate item.

### Deferred, with the trigger that would change that

- [ ] **Async transport.** Trigger: batch throughput measured as a problem. Today the client is
      sync — no pooling, calls cannot overlap. The `Transport` port makes it a contained change.
- [ ] **Retrieval / embeddings.** Trigger: a product need. Not added because job descriptions
      mention them.
- [ ] **Action-linking** (cut from the copilot plan) and **scenario question types** (pick one).

### Papercuts

- [ ] **Pricing constants are coupled to the model.** `INPUT_PRICE`/`OUTPUT_PRICE` in
      `run_evaluation.py` are the Sonnet figures. Setting `ANTHROPIC_MODEL` to Haiku without
      updating them makes the reported cost wrong; token counts stay accurate either way.
- [ ] **Docker not installed locally**, so the Testcontainers path runs only in CI.
- [ ] **Stale editor paths.** `OfferDelta-development-blueprint.md` is now `docs/BLUEPRINT.md`;
      reopen it if your IDE still has the old tab.

---

## Decisions recorded today

**The package stays `offerdelta`.** Renaming would touch every import, the Alembic config, the
Render service definition, and the live demo URL — no behaviour change, real chance of breaking a
working deployment. Documented in the README's *A note on naming* so it reads as a decision rather
than neglect. Only uncoupled text changed: OpenAPI title, page title, package description.

**The offer comparison is a scenario, not a separate product.** The taxonomy already implied it —
`RELOCATION_*` and `COMMUTE_*` exist because the engine answers relocation questions.

**A partial evaluation can never be quoted as a benchmark.** `--limit N` records `[:N]` in the
dataset version, and it appears in the report header.

---

*Convention: one file per working day in `docs/status/`, named `YYYY-MM-DD.md`.*
