"""Reference profiles for the public demo.

Lives in the source tree rather than under tests because the deployed service
serves this data. Tests import it from here too, so the demo and the golden
fixtures can never drift apart.
"""
